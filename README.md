# budget-pipeline
A multi-user budget system built around a deterministic, event-driven C++ pipeline that mirrors the data flow of sensor fusion pipelines. A Python service layer then handles the persistence, orchestration, and access control, with a PostgreSQL database.
